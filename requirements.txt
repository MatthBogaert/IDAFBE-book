# Pinned environment for the DA2026 course (Data Analytics for Business Engineers).
#
# Generated 2026-07-10 by resolving all packages fresh with pip (so every version below is
# mutually compatible — no manual guessing). numpy/pandas are the two versions that matter most:
#   - numpy 2.4.x: current major version.
#   - pandas is capped at <3 on purpose. pandas 3.0 was released very recently and changes
#     default behavior (mandatory copy-on-write, new string dtype handling) beyond what's been
#     verified against this course's notebooks. Revisit this cap once the data-heavy chapters
#     (data understanding, preparation, evaluation, modeling) have been checked against pandas 3.
#
# Install with: pip install -r requirements.txt

numpy==2.4.6
pandas>=2.3,<3
matplotlib==3.11.0
seaborn==0.13.2
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
feature_engine==1.9.4
sklearn_evaluation==0.12.2
pyarrow==25.0.0
openpyxl==3.1.5
jupyter==1.1.1
ipykernel==7.3.0
